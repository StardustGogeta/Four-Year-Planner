class CreatePlan extends React.Component {
    render() {
        return (
            <button onClick={this.props.onClick}>
                Click to create a standard plan
            </button>
        );
    }
}
